# Rust
/target
# Parallel builds use their own target directories to avoid fighting
# over cargo's lock when several models are being written at once.
/target-*

# Node
node_modules/
web/dist/
*.log
npm-debug.log*

# IDE and OS
.idea/
.vscode/
.DS_Store
Thumbs.db
